-- PropertyRadar HTML pilot batch (100 listings from local scrape + extract).
-- Rows use source_id = propertyradar and metadata->>'importSource' = 'pilot-html-100'.

COMMENT ON TABLE listings IS 'Unified listing inventory; PropertyRadar pilot batch tagged in metadata.importSource';
